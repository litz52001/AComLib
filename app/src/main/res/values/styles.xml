<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>
        <!--        <item name="android:windowBackground">@android:color/transparent</item>-->
    </style>

    <style name="ButtonTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorButtonNormal">#FF4081</item><!--正常状态下的颜色  -->
        <item name="colorControlHighlight">#9F4E4848</item><!--覆盖色，按下的颜色  -->
    </style>

    <style name="cusSpinner">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/shap_spinner_bg</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="basedialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 遮罩层 -->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <style name="UpdateDialog" parent="basedialog">
        <item name="android:windowEnterAnimation">@anim/update_app_window_in</item>  <!--显示动画-->
        <item name="android:windowExitAnimation">@anim/update_app_window_out</item> <!--消失动画-->
    </style>

    <!--adialog 渐变-->
    <style name="adialog_alpha" parent="basedialog">
        <item name="android:windowEnterAnimation">@anim/adialog_alpha_show</item>  <!--显示动画-->
        <item name="android:windowExitAnimation">@anim/adialog_alpha_cancel</item> <!--消失动画-->
    </style>

    <!--底部滑出-->
    <style name="bottomDialog" parent="basedialog">
        <item name="android:windowEnterAnimation">@anim/adialog_translate_show</item>  <!--显示动画-->
        <item name="android:windowExitAnimation">@anim/adialog_translate_cancel</item> <!--消失动画-->
    </style>

    <!--activity进场出场动画-->
    <style name="AnimationActivity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <style name="line_gray">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/line_gray</item>
    </style>

    <style name="line_gray_h">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/line_gray</item>
    </style>

    <style name="listv_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:cacheColorHint">@color/transparent</item>
        <item name="android:listSelector">@drawable/ripple_white_gray_bg</item>
        <item name="android:divider">@color/line_gray</item>
        <item name="android:transcriptMode">alwaysScroll</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:paddingLeft">1dp</item>
        <item name="android:paddingRight">1dp</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="grid_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:cacheColorHint">@color/transparent</item>
        <item name="android:horizontalSpacing">3dp</item>
        <item name="android:listSelector">@color/transparent</item>
        <item name="android:minHeight">70dp</item>
        <item name="android:numColumns">5</item>
        <item name="android:verticalSpacing">5dp</item>
    </style>


</resources>
